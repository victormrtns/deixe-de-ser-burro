import { useSuggestion } from './SuggestionProvider'
export function SuggestionActions() { const { state, accept, reject } = useSuggestion(); if (state === 'accepted') return <p role="status">Alteração aceita em uma nova versão.</p>; if (state === 'rejected') return <p role="status">Sugestão arquivada.</p>; return <div className="suggestion-actions"><button type="button" disabled={state === 'applying'} onClick={() => void accept()}>{state === 'applying' ? 'Aplicando…' : 'Aceitar alteração'}</button><button type="button" disabled={state === 'applying'} onClick={reject}>Descartar</button></div> }
